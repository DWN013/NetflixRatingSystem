import java.util.*;
import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.io.*;
/**
 * Write a description of class Main here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Main
{
    public static void Main (String[] args) 
    {
        BufferedReader reader = new BufferedReader(new FileReader("ratings.csv"));
        int userId = 1;
        ArrayList<int> movieId;
        ArrayList<double> userRating;
        String ratings;
        String[] ratingsFields;
        while ((ratings = reader.readLine()) != null) 
        {
            ratingsFields = movie.split(",");
            if (userId != Integer.parseInt(ratingsFields[0]))
            {
                User user = (userId, movieId, userRating);
                userId = Integer.parseInt(ratingsFields[0]);
            }
            moveId.add(Integer.parseInt(ratingsFields[1]));
            userRating.add(Double.parseDouble(ratingsFields[2]));
        }
    }
}
